# Generic Variables (shared by both pipeline environments)
# REPLACE: aws_region if you are not using us-east-1
aws_region       = "us-east-1"
business_divsion = "hr"

# REPLACE with YOUR Route 53 public hosted zone (do not use a domain you do not own)
route53_zone_name = "example.com"
