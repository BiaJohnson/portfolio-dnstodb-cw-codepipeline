# Local ignore extras (repo root .gitignore already covers secrets.tfvars / *.pem).
# CodeBuild generates secrets.tfvars from /CodeBuild/DB_PASSWORD and /CodeBuild/APP3_DB_PASSWORD.
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Generated by archive_file in c16-04 (URL baked from Route 53)
canary/*.zip
